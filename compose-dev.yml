# This compose file is for development only!
name: soat-tech-challenge-backend-dev
services:
  db:
    image: "postgres:15.4"
    environment:
      - "POSTGRES_DB=soat-tech-challenge-db"
      - "POSTGRES_PASSWORD=backend"
      - "POSTGRES_USER=backend"
    ports:
      - 5432:5432
