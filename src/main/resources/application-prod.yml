spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: ${DB_PASSWORD:backend}
        url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:tech_challenge}
        username: ${DB_USERNAME:backend}
    devtools:
        add-properties: false
